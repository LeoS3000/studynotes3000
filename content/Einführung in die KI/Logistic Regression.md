- Given labeled data ${(x_n, y_n)}$ with $x_n ∈ R$ and $y_n ∈ {0, 1}$, 
- minimize empirical risk with [[logistic loss]] $V_{logistic}$ 
- and hypothesis $f (x) = w^Tx + b$ 
- by fitting the training data

- data has to be [[linear separable]]
	- can be done by [[feature expansion]]

see also [[Business Analytics/Notes/Data Mining - Klassifikation]]